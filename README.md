# 슈퍼루키 SWAP 팀 용 데모를 보기 위한 레포지토리입니다.

DEMO 보러가기: [https://hamin-shim.github.io/swap-demo](https://rookie-swap.netlify.app/)

## 브랜치 현황

마지막 정리: 2026-07-10 KST

| 브랜치 | 상태 | 용도 / 포함 기능 |
| --- | --- | --- |
| `master` | 배포 기준 | Netlify 자동 배포 기준 브랜치. 현재 `dev`와 같은 커밋입니다. |
| `dev` | 통합 완료 | 현재 팀 검수용 기준 브랜치. NFC 활성화 음파 애니메이션, 50초 타이머, 결제 후 리포트, 시나리오 구조 개편까지 반영되어 있습니다. |
| `feature/gas-station-demo-scenario` | 작업 중 | 메인 데모를 GS칼텍스 주유소 시나리오로 교체하고 삼성 iD STATION 카드 추천 흐름을 검수하는 브랜치입니다. |
| `feature/copy-tone-and-location-simplify` | `dev` 반영 완료 | 팀 피드백 기반 카피 톤 정리, 위치 변경 시트 단순화, 기준 변경/위치 변경 적용 흐름 보강 작업 브랜치였습니다. |
| `feature/post-payment-system` | `dev` 반영 완료 | 결제 후 인사이트 리포트와 NFC 타이머 작업용 브랜치였습니다. 현재 `dev`/`master`와 같은 커밋입니다. |
| `feature/smart-stack-payment-flow` | `dev` 반영 완료 | 쿠폰 -> 멤버십 -> 결제 순서의 Smart Stack 결제 플로우 초안 작업 브랜치였습니다. 현재 내용은 `dev`에 포함되어 있고, 브랜치 자체는 과거 스냅샷으로 남아 있습니다. |
| `feature/scenario-structure-refresh` | `dev` 반영 완료 | 시나리오 구조 개편 브랜치였습니다. 위치 오인 시 매장 변경, 혜택 확인 하단 시트, 추천 기준 전환, `추천 이유 ?` 말풍선 UI가 반영되어 있습니다. |

## 브랜치 운영 규칙

- `master`: 실제 데모 배포용입니다. `dev`에서 검수가 끝난 뒤에만 올립니다.
- `dev`: 팀 공유 및 통합 검수용입니다. 기능 브랜치에서 충분히 확인한 뒤 merge합니다.
- `feature/*`: 기능이나 UX 실험 단위로 생성합니다. 작업 목적이 끝나면 `dev` 반영 여부를 위 표에 업데이트합니다.
- 새 작업을 시작할 때는 `dev`에서 새 브랜치를 파고, 작업이 끝나면 README의 브랜치 현황도 같이 갱신합니다.

## 최근 반영 내용

- 위치 오인 시 `다른 매장이에요`를 통해 인접 매장 후보로 바꿀 수 있습니다.
- 위치 후보를 고른 뒤에는 `이 매장으로 보기`를 눌러야 추천이 바뀝니다.
- 혜택 확인 화면은 하단 시트로 열리고, 필요한 카드/적립/예상 혜택/결제 버튼을 먼저 보여줍니다.
- 추천 기준은 `혜택`, `마일리지`, `실적`입니다.
- 추천 기준을 바꾸면 카드/혜택 변화를 먼저 보여주고, `이 기준으로 보기`를 눌러야 적용됩니다.
- 추천 이유는 `추천 이유 ?` 말풍선으로 확인합니다.

## 최신 시연 순서

1. Wallet 첫 화면에서 `GS칼텍스 삼평주유소에서 결제하시나요?`와 Top-1 추천을 확인합니다.
2. `추천 보기`를 눌러 추천 상세 하단 시트를 엽니다.
3. `다른 기준 보기`에서 추천 기준을 바꾸고, 카드/혜택 변화 미리보기를 확인한 뒤 `이 기준으로 보기`를 누릅니다.
4. `다른 매장이에요`를 눌러 SK에너지, S-OIL, EV 충전소 후보 중 하나를 고르고 `이 매장으로 보기`로 적용합니다.
5. 혜택 조합 결제를 선택해 적립/NFC 결제 흐름을 진행합니다.
6. 결제 후 결과 화면에서 `방금 결제로 4,500원 아꼈어요`, 다음 주유 실적까지 남은 금액, 카드사 앱 확인 CTA를 확인합니다.

## 시나리오 매핑

| 데모 기준 | 의도 | 코드 키 |
| --- | --- | --- |
| 혜택 | 포인트, 즉시할인, 남은 혜택을 묶어 가장 직관적인 절감 효과를 보여줍니다. | `max_benefit` |
| 마일리지 | 현금 할인보다 항공 마일리지 적립을 우선합니다. | `mileage` |
| 실적 | 다음 달 혜택 조건을 채우는 데 도움이 되는 카드를 우선합니다. | `performance_fill` |

## 메인 데모 카드

- 기본 위치: `GS칼텍스 삼평주유소`
- 1순위 추천: `삼성 iD STATION`
- 핵심 메시지: GS칼텍스 10% 결제일할인, 남은 월 한도, 전월 실적 조건, GS&POINT 적립을 Wallet 안에서 한 번에 정리합니다.
- 비교 카드: KB국민 주유패스, 신한 Deep Oil, 현대카드 M BOOST

## 현재 결정 로그

- 카드 실명은 발표 이해도를 위해 현재 데모에서는 유지합니다.
- 추가 카드 이미지는 이번 브랜치 범위에서는 넣지 않고, 이미지가 없는 카드는 fallback 카드 디자인을 사용합니다.
- 최신 Samsung Wallet 캡처는 필수 입력으로 두지 않고, 현재 S26 비율과 One UI식 하단 시트 패턴을 기준으로 검수합니다.
- 페이 플래너 별도 mock 화면은 만들지 않고, 카드사 앱 확인 CTA로 정리합니다.
- 회귀 검수는 `npm run qa`로 실행합니다.
